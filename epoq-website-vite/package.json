{
  "name": "nebula-app-studio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sitemap": "node scripts/generate-sitemap.js",
    "canonicals": "node scripts/inject-canonicals.js",
    "build": "npm run sitemap && npm run canonicals && vite build",
    "preview": "vite preview",
    "pre-deploy": "node scripts/pre-deploy.js",
    "deploy": "node scripts/deploy.js"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}